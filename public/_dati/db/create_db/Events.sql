CREATE TABLE IF NOT EXISTS lmgc_Events (
    id INT AUTO_INCREMENT PRIMARY KEY,
    action VARCHAR(50) NOT NULL,
    note VARCHAR(255),
    creationDate TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updateDate TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

INSERT INTO Event (action, note) VALUES ('CREATE', 'CREATE in SQL');
INSERT INTO Event (action, note) VALUES ('READ', 'SELECT in SQL');
INSERT INTO Event (action, note) VALUES ('UPDATE', 'UPDATE in SQL');
INSERT INTO Event (action, note) VALUES ('DELETE', 'DELETE in SQL');
