php_81_test:
  stage: testing
  image: php:8.1-cli
  script:
    - echo "Running tests on PHP 8.1..."
    - phpunit --configuration phpunit.xml
  except:
    changes:
      - $EXCLUDED_PATHS
  only:
    - branches
