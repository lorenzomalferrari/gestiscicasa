php_83_test:
  stage: testing
  image: php:8.3-cli
  script:
    - echo "Running tests on PHP 8.3..."
    - phpunit --configuration phpunit.xml
  except:
    changes:
      - $EXCLUDED_PATHS
  only:
    - branches
